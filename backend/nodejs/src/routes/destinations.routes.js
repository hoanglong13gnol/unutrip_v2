/**
 * Phase 2 compatibility shim — implementation moved to
 * `src/modules/destinations/destinations.routes.js`.
 * Kept as a re-export so any external import of this path keeps working.
 */
export { registerDestinationRoutes } from "../modules/destinations/destinations.routes.js";
